{
  "name": "jumperserver",
  "version": "0.0.1",
  "main": "./main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ JumperServer --all --out ~/Desktop/JumperServer --version 0.30.2 --overwrite --icon=./app/img/app-icon.icns"
  },
  "devDependencies": {
    "electron-packager": "^5.0.1",
    "electron-prebuilt": "^0.30.3"
  },
  "dependencies": {
    "jquery": "^3.1.1",
    "nconf": "^0.7.2"
  }
}
